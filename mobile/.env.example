# API server URL — only needed for native (iOS/Android) builds.
# The web version uses relative URLs automatically.
# Set this to your server's LAN IP when running on a physical device.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
